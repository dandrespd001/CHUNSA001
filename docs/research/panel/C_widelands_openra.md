[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input =[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay diverg[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- |[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C#[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] |[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ |[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ | GPLv1+ [?] | Si [?] | Si[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ | GPLv1+ [?] | Si [?] | Si |
| Seven Kingdoms[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ | GPLv1+ [?] | Si [?] | Si |
| Seven Kingdoms [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |

[Free RTS Games to Play For Free in 2026](https://www.youtube.com/watch?v=A2SPeCh2s2A)

Este video es relevante porque ofrece una panorámica actualizada de juegos y motores RTS libres[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ | GPLv1+ [?] | Si [?] | Si |
| Seven Kingdoms [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |

[Free RTS Games to Play For Free in 2026](https://www.youtube.com/watch?v=A2SPeCh2s2A)

Este video es relevante porque ofrece una panorámica actualizada de juegos y motores RTS libres y comunitarios activos en 2026.[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ | GPLv1+ [?] | Si [?] | Si |
| Seven Kingdoms [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |

[Free RTS Games to Play For Free in 2026](https://www.youtube.com/watch?v=A2SPeCh2s2A)

Este video es relevante porque ofrece una panorámica actualizada de juegos y motores RTS libres y comunitarios activos en 2026.[?] Widelands define cadenas de produccion declarando inputs y outputs en ficheros Lua de edificios -> [CHUNSA] el motor lee que bienes consume y produce cada sitio de trabajo mediante tablas asociadas.
[?] Ejemplo fichero Widelands (incompleto/inferido por estructura tipica Lua de edificios): input = { "log" }, output = { "planks" } -> [CHUNSA] la IA economica gestiona las cadenas de suministros con esta asignacion de inventarios.
[?] OpenRA implementa multijugador determinista mediante el modelo lockstep estricto enviando solamente ordenes de comandos de entrada en vez de estados completos -> [CHUNSA] se reduce al minimo el ancho de banda requerido por red independientemente de las unidades.
[?] OpenRA usa bufers de retraso de reproduccion para sincronizar la llegada de paquetes de comandos por tick de simulacion -> [CHUNSA] se previenen discrepancias temporales entre los clientes conectados.
[?] OpenRA detecta desincronizaciones comparando hashes de estado o hashes de ordenes ejecutadas en red de forma periodica entre nodos -> [CHUNSA] si hay divergencia, salta un error de fuera de sincronizacion (out of sync).
[?] OpenRA emplea aritmetica entera y de punto fijo para evitar las divergencias introducidas por el soporte nativo de punto flotante en distintas arquitecturas de CPU -> [CHUNSA] se garantiza la determinacion matematica exacta en todas las plataformas.

| Motor RTS | Lenguaje | Licencia | Simulacion Determinista | Motor Data-Driven |
| :--- | :--- | :--- | :--- | :--- |
| Spring / Recoil [?] | C++ [?] | GPLv2 [?] | Si [?] | Si [?] |
| MegaGlest [?] | C++ [?] | GPLv3 [?] | Si [?] | Si [?] |
| Warzone 2100 [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |
| OpenRA | C# | GPLv3 [?] | Si | Si [?] |
| 0 A.D. [?] | C++ / JS [?] | GPLv2+ [?] | Si [?] | Si [?] |
| openage [?] | C++ / Python [?] | GPLv3+ [?] | Si [?] | Si [?] |
| Widelands | C++ | GPLv1+ [?] | Si [?] | Si |
| Seven Kingdoms [?] | C++ [?] | GPLv2+ [?] | Si [?] | Si [?] |

[Free RTS Games to Play For Free in 2026](https://www.youtube.com/watch?v=A2SPeCh2s2A)

Este video es relevante porque ofrece una panorámica actualizada de juegos y motores RTS libres y comunitarios activos en 2026.
http://googleusercontent.com/youtube_content/1
